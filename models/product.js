class Product {
  constructor(id, ownerId, title, images, category, description, price) {
    this.id = id;
    this.ownerId = ownerId;
    this.title = title;
    this.images = images;
    this.category = category;
    this.description = description;
    this.price = price;
  }
}

export default Product;
