class CartItem {
  constructor(quanity, productPrice, productTitle, sum) {
    this.quanity = quanity;
    this.productPrice = productPrice;
    this.productTitle = productTitle;
    this.sum = sum;
  }
}

export default CartItem;
